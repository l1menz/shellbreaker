{
  "name": "shellbreaker-backend",
  "version": "1.0.0",
  "private": true,
  "description": "ShellBreaker API - FastAPI backend",
  "scripts": {
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}",
    "build": "pip install -r requirements.txt"
  }
}
